% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance_closure.R
\name{balance_closure_test}
\alias{balance_closure_test}
\title{Run the Balance Closure Test}
\usage{
balance_closure_test(
  sim_rds,
  output_dir = NULL,
  usms = NULL,
  parallel = FALSE,
  cores = NA,
  verbose = 1L
)
}
\arguments{
\item{sim_rds}{path to an \code{.rds} file containing pre-computed
simulation data, including the balance variables — see
\code{\link{run_simulations}}'s \code{vars} argument. The USMs to test
are the names of this list, optionally restricted by \code{usms}}

\item{output_dir}{directory where balance error details
(\code{balance_errors_details.csv}) will be written, if any. If NULL
(default), details are not exported}

\item{usms}{character vector of USMs to restrict the test to. If NULL
(default), all USMs found in \code{sim_rds} are tested}

\item{parallel}{Boolean. Is the computation to be done in parallel ?}

\item{cores}{Number of cores to use for parallel computation}

\item{verbose}{Integer. Logging verbosity level: 0 = silent, 1 = info,
2 = debug}
}
\value{
Invisibly, the internal test object. Called for its side
effects: logging a summary of the test, exporting detailed balance
errors to \code{output_dir} (if defined), and stopping with an error if any
USM failed the test.
}
\description{
Checks that the initial and final water and nitrogen balances are equal
(or NA) for each USM, using pre-computed simulation data from
\code{sim_rds} — see \code{\link{run_simulations}} to produce it. If
there are discrepancies, it logs a warning with the details of the issue
and stops with an error listing the affected USMs.
}
\examples{
\dontrun{
balance_closure_test(
  sim_rds    = "/path/to/simulations.rds",
  output_dir = "/path/to/output_dir",
  parallel   = TRUE,
  cores      = 4
)
}
}
