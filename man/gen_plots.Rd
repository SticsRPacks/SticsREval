% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{gen_plots}
\alias{gen_plots}
\title{Generate comparison and scatter plots for each species}
\usage{
gen_plots(
  config,
  workspace = EvalWorkspace$new(config$eval_workspace),
  backend = ParallelBackend$new(config$parallel, config$cores),
  scatter_fn = gen_scatter_plot,
  comparison_fn = function(x, dir) x$plot_comparison(dir),
  logger_info = logger::log_info
)
}
\arguments{
\item{config}{List. Configuration object created by \code{make_config()},
containing all parameters required for the plots generation}

\item{workspace}{EvalWorkspace. An instance of \code{EvalWorkspace} to access the
evaluation data. Defaults to a new instance using the \code{eval_workspace}
parameter from the configuration.}

\item{backend}{ParallelBackend. An instance of \code{ParallelBackend} to run
parallel computations. Defaults to a new instance using the \code{parallel} and
\code{cores} parameters from the configuration.}

\item{scatter_fn}{Function. A function to generate scatter plots. It should
accept the following arguments: \code{output_dir}, \code{sim}, \code{obs}, \code{ref_sim}, and
\code{vars}. Defaults to \code{gen_scatter_plot()}.}

\item{comparison_fn}{Function. A function to generate comparison plots. It
should accept the following arguments: \code{comparison} and \code{output_dir}.
Defaults to a function that calls the \code{plot_comparison()} method of the
comparison object.}

\item{logger_info}{Function. A logging function to report progress. It should
accept a single string argument. Defaults to \code{logger::log_info()}.}
}
\value{
NULL. This function is called for its side effects (writing plot
files).
}
\description{
This function generates diagnostic plots for each species found in the
evaluation workspace. It produces comparison plots and, when possible,
scatter plots highlighting deteriorated variables based on a given threshold.
}
\details{
For each species, the function:
\itemize{
\item Retrieves comparison data between simulation and observation.
\item Generates comparison plots.
\item Identifies deteriorated variables using a threshold percentage.
\item Optionally generates scatter plots if reference simulation data
are available.
}

Scatter plots are only generated when both reference simulation data and
deteriorated variables are available.

For each species:
\itemize{
\item Creates a species-specific output directory.
\item Generates comparison plots using \code{gen_comparison_plot()}.
\item Identifies critical and warning variables using the given percentage.
\item Generates scatter plots using \code{gen_scatter_plot()} if reference data
and deteriorated variables are available.
}
}
\examples{
\dontrun{
config <- make_config(
 output_dir = "results/",
 reference_workspace = "ref_dir/"
)
gen_plots(config)
}

}
