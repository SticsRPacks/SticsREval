% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usm_evaluation.R
\name{USMEvaluation}
\alias{USMEvaluation}
\title{USMEvaluation class}
\description{
Internal class. Detects USMs (situations) where the model performance
on a given variable is very degraded compared to a reference version,
relative to the overall performance of the species on that variable.
Instantiated internally by \code{\link{evaluate}}.
}
\details{
For each variable and each USM, the following ratio is computed:

\deqn{RMSE\_ratio = 100 \times
\frac{RMSE_{eval} - RMSE_{ref}}{RMSE_{species}}}

where:
\itemize{
\item \code{RMSE_eval}: RMSE of the evaluated version, for the variable,
on that USM.
\item \code{RMSE_ref}: RMSE of the reference version, for the variable,
on that USM.
\item \code{RMSE_species}: RMSE of the evaluated version, for the
variable, computed on the whole species (all USMs together).
}

A USM is considered as \strong{not passing the test} if, for at least one
variable:
\itemize{
\item \code{RMSE_ratio > ratio_threshold} (default 50\%), OR
\item more than \code{max_degraded_vars} (default 3) variables have
\code{RMSE_ratio > degraded_threshold} (default 20\%) on that USM.
}

USMs/variables with less than 10 observations (\code{n_obs}) are ignored,
consistently with \code{RRmseComparison}/\code{DeterioratedUSMComparison}.

The class can be used in two ways:
\itemize{
\item Driven by \code{eval_workspace}/\code{workspace}, like
\code{SpeciesEvaluation}: create with \code{eval_workspace} (or a
pre-built \code{workspace}), then call \code{run()}.
\item Manually, by directly providing \code{stats_usm} and
\code{stats_species} (or a pre-computed \code{data}) for a single
species, useful for unit testing.
}
}
\keyword{internal}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{degraded_vars}}{Data frame of variable/USM combinations (across all species) where
\code{rmse_ratio} exceeds \code{degraded_threshold} (default 20\%).}

    \item{\code{critical_vars}}{Data frame of variable/USM combinations (across all species) where
\code{rmse_ratio} exceeds \code{ratio_threshold} (default 50\%).}

    \item{\code{failed_usms}}{Data frame with columns \code{species}/\code{situation} for USMs that
do not pass the test: either at least one variable with a ratio above
\code{ratio_threshold}, or more than \code{max_degraded_vars} variables
above \code{degraded_threshold}.}

    \item{\code{success}}{TRUE if the USM evaluation is successful, i.e. no USM failed the test
for any species.}

    \item{\code{is_empty}}{TRUE if there is no data to evaluate.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-USMEvaluation-initialize}{\code{USMEvaluation$new()}}
    \item \href{#method-USMEvaluation-run}{\code{USMEvaluation$run()}}
    \item \href{#method-USMEvaluation-get_data}{\code{USMEvaluation$get_data()}}
    \item \href{#method-USMEvaluation-export}{\code{USMEvaluation$export()}}
    \item \href{#method-USMEvaluation-summary}{\code{USMEvaluation$summary()}}
    \item \href{#method-USMEvaluation-clone}{\code{USMEvaluation$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-USMEvaluation-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-USMEvaluation-initialize}{}}}
\subsection{\code{USMEvaluation$new()}}{
  Create a new USMEvaluation object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{USMEvaluation$new(
  eval_workspace = NULL,
  species_filter = NULL,
  usms = NULL,
  var2exclude = NULL,
  percentage = 5,
  output_dir = NULL,
  parallel = FALSE,
  cores = NA,
  workspace = NULL,
  backend = NULL,
  logger = default_logger,
  species_evaluation = NULL,
  ratio_threshold = 50,
  degraded_threshold = 20,
  max_degraded_vars = 3,
  species = NULL,
  stats_usm = NULL,
  stats_species = NULL,
  data = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{eval_workspace}}{Path to the evaluation workspace. Only used to
build a default \code{workspace} when one isn't supplied. Providing this
or \code{workspace} selects the evaluation mode; otherwise the object is
built in manual mode from \code{data}/\code{stats_usm}+\code{stats_species}.}
      \item{\code{species_filter}}{Optional character vector of species to
evaluate.}
      \item{\code{usms}}{Optional character vector of USMs to evaluate.}
      \item{\code{var2exclude}}{Optional character vector of variables to exclude.}
      \item{\code{percentage}}{Threshold (\%) above which a variable is flagged as
deteriorated vs. the reference.}
      \item{\code{output_dir}}{Output directory for the CSV export.}
      \item{\code{parallel, cores}}{Parallel execution options. Only used to build
a default \code{backend} when one isn't supplied.}
      \item{\code{workspace}}{An optional EvalWorkspace object. If not provided, a
new EvalWorkspace will be created from \code{eval_workspace}.}
      \item{\code{backend}}{An optional ParallelBackend object for parallel
processing. If not provided, a new ParallelBackend will be created
from \code{parallel}/\code{cores}.}
      \item{\code{logger}}{An optional logger object for logging messages. If not
provided, the default logger will be used.}
      \item{\code{species_evaluation}}{Optional \code{SpeciesEvaluation} instance.
If provided and already \code{run()}, its per-species statistics are
reused instead of being recomputed from scratch (avoids re-reading
the sim/obs/ref data and re-running CroPlotR's \code{summary()} for
each species). Falls back to computing statistics independently for
any species not found in \code{species_evaluation} (e.g. if it
hasn't been run yet).}
      \item{\code{ratio_threshold}}{Numeric threshold (\%) above which a single
variable makes the USM fail. Default 50.}
      \item{\code{degraded_threshold}}{Numeric threshold (\%) above which a variable
is considered degraded. Default 20.}
      \item{\code{max_degraded_vars}}{Maximum number of degraded variables (ratio >
\code{degraded_threshold}) tolerated per USM before it is considered
failed. Default 3 (i.e. fails starting at 4 degraded variables).}
      \item{\code{species}}{Optional species name, used in manual mode (ignored in
evaluation mode).}
      \item{\code{stats_usm}}{Manual mode: data frame of per-USM stats (RMSE,
n_obs), for \code{evaluated} and \code{reference} groups, e.g. as
returned by CroPlotR's summary method with \code{all_situations =
FALSE, stats = c("RMSE", "n_obs")}.}
      \item{\code{stats_species}}{Manual mode: data frame of species-level stats
(RMSE), e.g. as returned by CroPlotR's summary method with
\code{all_situations = TRUE} (default).}
      \item{\code{data}}{Manual mode: optional pre-computed data frame (bypasses
\code{stats_usm}/\code{stats_species} computation).}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-USMEvaluation-run"></a>}}
\if{latex}{\out{\hypertarget{method-USMEvaluation-run}{}}}
\subsection{\code{USMEvaluation$run()}}{
  Run the USM evaluation.
This method computes, for each species found in the workspace
(filtered according to \code{species_filter}/\code{usms}), the RMSE_ratio per
variable and per USM, and stores the results internally.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{USMEvaluation$run()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-USMEvaluation-get_data"></a>}}
\if{latex}{\out{\hypertarget{method-USMEvaluation-get_data}{}}}
\subsection{\code{USMEvaluation$get_data()}}{
  Return the underlying data frame (one row per variable/USM/species
combination, with columns: species, situation, variable, rmse_eval,
rmse_ref, rmse_species, ratio, n_obs).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{USMEvaluation$get_data()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-USMEvaluation-export"></a>}}
\if{latex}{\out{\hypertarget{method-USMEvaluation-export}{}}}
\subsection{\code{USMEvaluation$export()}}{
  Export the USM evaluation data for deteriorated USMs to a CSV file.

The exported file is written to the \code{csv} subdirectory of the
configured output directory and is named \code{Deteriorated_USM.csv}. Only
USMs that contain evaluation data are included in the export.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{USMEvaluation$export()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-USMEvaluation-summary"></a>}}
\if{latex}{\out{\hypertarget{method-USMEvaluation-summary}{}}}
\subsection{\code{USMEvaluation$summary()}}{
  Print a summary of the USM evaluation results, species by species.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{USMEvaluation$summary()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-USMEvaluation-clone"></a>}}
\if{latex}{\out{\hypertarget{method-USMEvaluation-clone}{}}}
\subsection{\code{USMEvaluation$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{USMEvaluation$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
