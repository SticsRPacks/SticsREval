% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_species_sim}
\alias{export_species_sim}
\title{Export simulation data per species to Parquet files}
\usage{
export_species_sim(output_dir, eval_workspace = DEFAULT_WORKSPACE)
}
\arguments{
\item{output_dir}{Character. Path to the directory where output files will
be saved.}

\item{eval_workspace}{Character. Path to the evaluation workspace containing
simulation data. Defaults to \code{DEFAULT_WORKSPACE}.}
}
\value{
NULL. This function is called for its side effects (writing files).
}
\description{
This function exports simulation datasets for each species found in the
evaluation workspace into Parquet files. For every species, it creates
an output directory (if needed) and writes the corresponding simulation
data as a Parquet file.
}
\details{
Simulation data are retrieved using \code{get_by_species()} and written using
\code{arrow::write_parquet()}.

For each species:
\itemize{
\item Creates a species-specific output directory.
\item Retrieves simulation data.
\item Writes the data to a \code{Simulations.parquet} file.
}
}
\examples{
\dontrun{
export_species_sim("results/", eval_workspace = "workspace/")
}

}
