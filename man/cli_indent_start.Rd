% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cli_indent_start}
\alias{cli_indent_start}
\title{Open a \code{cli} container that indents \code{cli_dl()} and \code{cli_li()} calls
printed inside it by two spaces, matching \code{cli_species_rule()}'s indent.
\code{cli_text()}/\verb{cli_alert_*()} calls placed \emph{after} a \code{cli_li()} in the
same block also come out correctly indented, for a status line with no
\code{cli_li()} anywhere before it, use \code{cli_indent_text()} instead.}
\usage{
cli_indent_start(.envir = parent.frame())
}
\description{
A plain \code{cli_div()} with a "margin-left" theme renders fine for \code{cli_dl()},
but RStudio's console has its own rendering of \code{cli} bullets that ignores
such a margin, so \code{cli_li()} items end up flush left. Using \code{cli}'s native
list nesting instead (one \code{cli_ul()} inside another, with no item on the
outer one) sidesteps that: it's the indent \code{cli_li()} itself understands,
so it renders consistently everywhere.
}
\details{
Close with \code{cli_indent_end()} on the returned ids. \code{.envir} must resolve
to the caller's frame (not this function's own), since \code{cli}
containers auto-close once that frame exits.
}
