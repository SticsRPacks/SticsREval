% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cli_indent_text}
\alias{cli_indent_text}
\title{Print already-interpolated \code{text} verbatim, indented by \code{margin} spaces.}
\usage{
cli_indent_text(text, margin = 2)
}
\description{
Only for a status line with no \code{cli_li()} bullet anywhere before it in
the current indented block (e.g. a lone "all passed" message) - such a
line otherwise ends up flush left, because \code{cli::cli_text()}/
\verb{cli_alert_*()} strip leading whitespace and only pick up a container's
left margin once a \code{cli_li()} has actually been drawn inside it.
\code{cli_verbatim()} never reformats its input, so a manual prefix always
renders correctly here - but for the same reason it does \emph{not} do glue
interpolation (\code{text} must already be a plain string, e.g. built with
\code{sprintf()}), and if a \code{cli_li()} \emph{has} already run in the block,
\code{cli_verbatim()} picks up the container's margin on top of this manual
one and ends up double-indented - use plain \code{cli::cli_text()} there
instead, which by then indents correctly on its own.
}
