% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/configuration.R
\name{make_config}
\alias{make_config}
\title{Create configuration for the evaluation workflow}
\usage{
make_config(
  stics_exe = NULL,
  workspace = NULL,
  metadata_file = NULL,
  run_simulations = TRUE,
  verbose = 1,
  parallel = FALSE,
  cores = NA,
  reference_workspace = NULL,
  percentage = 5,
  eval_workspace = DEFAULT_WORKSPACE,
  init_workspace = TRUE,
  output_dir = NULL,
  species = NULL,
  usms = NULL,
  var2exclude = NULL,
  force = FALSE
)
}
\arguments{
\item{stics_exe}{Character. Path to the STICS executable.}

\item{workspace}{Character. Path to the working directory containing
simulation inputs.}

\item{metadata_file}{Character. Path to the metadata file describing
simulations.}

\item{run_simulations}{Logical. Whether to run simulations.
Defaults to TRUE.}

\item{verbose}{Integer. Verbosity level for logging. Defaults to 1.}

\item{parallel}{Logical. Whether to enable parallel execution.
Defaults to FALSE.}

\item{cores}{Integer or NA. Number of cores to use for parallel
execution. If NA, the number of available cores may be used.}

\item{reference_workspace}{Character or NULL. Path to reference
evaluation data. If NULL, reference-based analyses may be skipped.}

\item{percentage}{Numeric. Threshold used for evaluation metrics
(e.g., detecting deteriorated variables). Defaults to 5.}

\item{eval_workspace}{Character. Path to the evaluation workspace.
Defaults to \code{DEFAULT_WORKSPACE}.}

\item{init_workspace}{Logical. Whether to initialize the evaluation
workspace. Defaults to TRUE.}

\item{output_dir}{Character or NULL. Path to the output directory
for exported files. Defaults to NULL.}

\item{species}{Character vector or NULL. Optional list of species to
evaluate. If NULL, all available species are evaluated.
Defaults to NULL.}

\item{usms}{Character vector or NULL. Optional list of USMs to
evaluate. If NULL, all available USMs are evaluated.
Defaults to NULL.}

\item{var2exclude}{Character vector or NULL. Optional list of variables
to exclude from evaluation. If NULL, all available variables are
evaluated.
Defaults to NULL.}

\item{force}{Logical. Whether to overwrite an existing non-empty
evaluation workspace without prompting. Defaults to FALSE.}
}
\value{
A named list containing the configuration parameters.
}
\description{
This function builds a configuration list containing all parameters
required to run simulations and evaluation workflows.
}
\details{
The resulting configuration object can be passed to downstream functions
to ensure consistent parameter handling.
}
\examples{
\dontrun{
config <- make_config(
  stics_exe = "/path/to/stics",
  workspace = "workspace/",
  metadata_file = "metadata.csv"
)
}

}
